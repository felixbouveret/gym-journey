import { Text } from "native-base";

import { RootTabScreenProps } from "@/types";

export default function ProgramsScreen({}: RootTabScreenProps<"Programs">) {
  return <Text>Vos programmes</Text>;
}
